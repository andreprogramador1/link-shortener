import styled from 'styled-components'

export const Container = styled.div`
  display: flex;
  flex-direction: column;

  svg {
    overflow: hidden;
    transform: scale(0.7)
    translateX(-100px);
  }
`;